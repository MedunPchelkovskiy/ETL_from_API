services:
  weather-etl:
    build:
      context: .
      dockerfile: Dockerfile
    image: weather-etl:latest
    container_name: weather-etl-test
    env_file:
      - .env.cloud
    networks:
      - monitoring
networks:
  monitoring:
    external: true







#services:
#  weather-etl:
#    image: weather-etl:latest
#    container_name: weather-etl-test
#    env_file:
#      - .env.cloud
#    networks:
#      - monitoring
#
#networks:
#  monitoring:
#    external: true