prefect-version: "3.*"
name: etl_from_api

deployments:
  - name: local-dev
    description: Local Prefect deployment
    entrypoint: src.flows.bronze.extract_data_for_ski_resorts_in_Bulgaria:weather_flow_run
    tags: [local, portfolio]
    parameters:
      debug: true
    schedule:
      cron: "13 * * * *"
      timezone: Europe/Sofia
    concurrency_limit: 1
    work_pool:
      name: local-dev-process
